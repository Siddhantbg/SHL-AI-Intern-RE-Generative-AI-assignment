# Minimal dependencies for Render free tier (512MB RAM)
fastapi>=0.104.1,<0.110.0
uvicorn>=0.24.0,<0.30.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# LLM integration - only Gemini (lightweight)
google-generativeai>=0.3.2,<1.0.0

# Essential utilities only
python-dotenv>=1.0.0,<2.0.0

# Removed heavy ML dependencies:
# - sentence-transformers (uses ~200MB RAM)
# - faiss-cpu (uses ~100MB RAM) 
# - numpy (uses ~50MB RAM)
# - torch/transformers (uses ~300MB+ RAM)
#
# Total memory savings: ~650MB+
# Now using simple text similarity instead of ML embeddings